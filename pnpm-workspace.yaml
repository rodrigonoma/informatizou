packages:
  - 'packages/*'
  - 'apps/*'

# Dependências cujos build scripts têm permissão para rodar.
# esbuild é usado por vitest/tsx e precisa compilar seu binário nativo.
allowBuilds:
  esbuild: true
