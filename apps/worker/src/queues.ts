// Reexporta os nomes/conexão das filas do pacote compartilhado (§39).
export {
  QUEUE_NAMES,
  ALL_QUEUE_NAMES,
  parseRedisConnection,
  buildDefaultJobOptions,
  type QueueName,
} from '@informatizou/queue';
